<phone:PhoneApplicationPage
                            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
                            xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
                            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                            xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
                            xmlns:HubNews="clr-namespace:HubNews" x:Class="HubNews.MainPage"
                            FontFamily="{StaticResource PhoneFontFamilyNormal}"
                            FontSize="{StaticResource PhoneFontSizeNormal}"
                            Foreground="{StaticResource PhoneForegroundBrush}"
                            SupportedOrientations="Portrait"
                            Orientation="Portrait"
                            mc:Ignorable="d"
                            d:DesignWidth="480"
                            d:DesignHeight="768"
                            shell:SystemTray.IsVisible="True"
                            DataContext="{Binding Source={StaticResource Locator}, Path=Main}">

    <phone:PhoneApplicationPage.Resources>
        <DataTemplate x:Key="HubNews.ViewModels.FeedGridPanoramaItemViewModel">
            <ListBox ItemsSource="{Binding Items}">

            </ListBox>
        </DataTemplate>
        <DataTemplate x:Key="HubNews.ViewModels.FeedListPanoramaItemViewModel">
            <ListBox ItemsSource="{Binding Items}">

            </ListBox>
        </DataTemplate>
    </phone:PhoneApplicationPage.Resources>

    <!--LayoutRoot contains the root grid where all other page content is placed-->
	<Grid x:Name="LayoutRoot"
	      Background="Transparent">
		<Grid.ColumnDefinitions>
	        <ColumnDefinition Width="*" />
	    </Grid.ColumnDefinitions>
	    <Grid.RowDefinitions>
	        <RowDefinition Height="10" />
			<RowDefinition Height="*" />
	    </Grid.RowDefinitions>
		
		<ProgressBar Grid.Row="0" IsIndeterminate="True" />
		
		<controls:Panorama ItemsSource="{Binding PanoramaItems}"
                            Title="Hub News"
							Height="Auto" Width="Auto"
							Grid.Row="1">
			<controls:Panorama.TitleTemplate>
		    	<DataTemplate>
		      		<StackPanel Height="170" Orientation="Vertical">
		        		<TextBlock 	Text="{Binding}"
									FontSize="50" Margin="0,65,0,0"
									/>
						<TextBlock 	Text="Restez connecté!"
									FontSize="20" Margin="70,0,0,0"
									Style="{StaticResource PhoneTextAccentStyle}"
									FontStyle="Italic" />
		      		</StackPanel>
		    	</DataTemplate>
		  	</controls:Panorama.TitleTemplate>

            <controls:Panorama.ItemTemplate>
                <DataTemplate>
                    <HubNews:DataTemplateSelector Content="{Binding}" />
                </DataTemplate>
            </controls:Panorama.ItemTemplate>
        </controls:Panorama>
    </Grid>

</phone:PhoneApplicationPage>